export function addPinnedId(pinnedIds: string[], id: string): string[];
export function removePinnedId(pinnedIds: string[], id: string): string[];
export function togglePinnedId(pinnedIds: string[], id: string): string[];
export function transferPinnedId(
  pinnedIds: string[],
  fromId: string,
  toId: string,
): string[];

